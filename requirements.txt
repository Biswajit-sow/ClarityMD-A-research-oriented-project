# --- Core ML & Data Science ---
# Pinning PyTorch versions is crucial for stability on Streamlit Cloud
torch==2.1.0
torchvision==0.16.0
# Let PyTorch Lightning handle its specific dependencies
pytorch-lightning
# Pin Captum to a known stable version
captum==0.7.0
scikit-learn
imbalanced-learn
pandas
# Pin numpy to the version compatible with captum
numpy==1.26.4
# Use a specific, stable version of OpenCV
opencv-python-headless==4.8.0.76
Pillow
matplotlib
scikit-image
# Pinning TensorBoard and its dependency protobuf is a common fix
tensorboard
protobuf==3.20.0
PyYAML

# --- Image Augmentation ---
albumentations

# --- Web Frameworks & Frontend ---
streamlit
gradio

# --- LLM Integration ---
langchain
langchain-groq
python-dotenv